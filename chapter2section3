简单整理已经导入的数据
1.变量标签
例如gender，可能会想加入一个标签使得用户能够一眼看清楚其中分为male和female，gender是变量名，male和female是变量标签
对变量名称的处理比较有限，一种方法是将变量的标签直接作为变量名
names(data[2])  <- "gender(male or female)" 
将data数据集的第二向重新命名为后面这一串。显然这个方法并不是很理想。

2.值标签
函数factor可以为类别型变量创建值标签。例如gender有两类，那么
data$gender <- factor(data$gender, levels=c(1,2), labels=c("male","female")
levels代表了变量的实际值，labels表示值标签的字符变量

3.简单处理数据
length()  对象中元素的数量（几行）
dim()  对象的维度
str()  对象的结构
class()  对象的类型
mode()  对象的模式
names()  对象中各成分的名称（列名）
c()  将几个对象合并为一个向量
cbind()  将几个对象按列合并
rbind()  将几个对象按行合并
head()  列出对象开始的一部分
tail()  列出对象最后的一部分
ls()  显示当前对象列表
rm()  移除工作环境中的一个或多个对象
fix()  直接编辑对象，与edit类似，但不用重复赋值

后续会使用向量、矩阵、数据框、列表等各种数据模式，选取元素、取子集、变换模式等操作都非常重要
对数据的转化有助于分析，第四章会讲解创建新变量、变换和重编码已有的变量、合并数据集、观测等
导入有很多方式，导出其实也是，在附录中有介绍
